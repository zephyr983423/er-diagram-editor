{
  "name": "er-diagram-editor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A browser-based Merise entity-association diagram editor.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "konva": "^9.3.22"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
